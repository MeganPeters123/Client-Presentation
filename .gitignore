# Holds internal network paths — keep out of the public repo.
# The glob matters: a config.json.bak from an edit is just as sensitive as the original.
scripts/config.json*

# Generated history / working data — never commit real fund data
*history*.json
archive/

__pycache__/
*.pyc
